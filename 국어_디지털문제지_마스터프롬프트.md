# 📋 국어 디지털 문제지 생성기 — 마스터 프롬프트

> **사용법**: 아래 프롬프트를 복사해서 Claude, GPT-4o, Gemini 등 어느 AI에나 붙여넣고,
> PDF 파일(또는 텍스트로 변환한 내용)을 함께 첨부하면 됩니다.

---

## ✅ 마스터 프롬프트 (복사해서 바로 사용)

```
당신은 한국어 수능/EBS 문제지를 디지털 인터랙티브 HTML로 변환하는 전문가입니다.
아래 지침을 정확히 따라 단일 HTML 파일을 생성하세요.

---

## STEP 1. PDF/문서에서 추출할 항목

PDF를 분석하여 아래 항목을 정확히 구분해 추출합니다:

1. **지문 정보**
   - 문항 범위 표시 (예: "[01~04] 다음 글을 읽고 물음에 답하시오.")
   - 출전 및 제목 (있으면)
   - 본문 단락 (들여쓰기, 줄바꿈 보존)
   - 밑줄 조건: ㉠㉡㉢㉣㉤ 등으로 표시된 구절
   - 괄호 구간: [A], [B] 등 특정 구간 표시
   - 각주 (※ 기호로 시작하는 설명)
   - 그림/표/도식 (텍스트로 최대한 재현)

2. **문항 구조** (문항마다 반복 추출)
   - 문항 번호 (1, 2, 3...)
   - 문항 코드 (예: [26002-0172])
   - 발문 (질문 본문)
   - 〈보기〉 내용 (있는 경우만)
   - 선택지 ①②③④⑤와 각 텍스트
   - **정답 번호** (반드시 확인)
   - **정답 해설** (왜 맞는지)
   - **오답 해설** (왜 틀렸는지, 선택지별 또는 일괄)

---

## STEP 2. HTML 구조 설계

단일 .html 파일로 생성하며, 외부 의존성은 Google Fonts와 CDN만 허용합니다.

### 필수 구성 요소

```html
<!-- 1. 고정 헤더 (sticky) -->
<header>
  - 시험명 / 지문 제목
  - 현재 점수 표시 (X/Y 형식)
  - 초기화 버튼
</header>

<!-- 2. 지문 박스 -->
<section class="passage-box">
  - 문항 범위 표시
  - 본문 텍스트 (단락별 <p> 태그)
  - 밑줄 조건: <span class="cond">㉠...</span>
  - 괄호 구간: class="bracket-a"로 감싸기
  - 그림: class="figure-wrap"으로 구성
  - 각주: class="footnotes"
</section>

<!-- 3. 문항 카드 (문항 수만큼 반복) -->
<article class="question-card" id="card-N">
  <header class="q-header">
    - 문항번호 원형 뱃지
    - 발문 텍스트
    - 문항 코드
    - 정오답 상태 아이콘 (초기: ○)
  </header>
  <div class="q-body">
    - [선택적] 〈보기〉 박스
    - 선택지 버튼들 (onclick="answer(N, 선택번호, 정답번호)")
    - 결과 표시 바 (초기: 숨김)
  </div>
</article>

<!-- 4. 최종 결과 패널 (모든 문항 답변 후 표시) -->
<div class="final-score">
  - 최종 점수 (X / 전체)
  - 격려 메시지
  - 진행률 바
</div>
```

---

## STEP 3. CSS 디자인 시스템

아래 CSS 변수를 반드시 사용합니다 (시험지 감성):

```css
:root {
  --bg: #f5f0e8;          /* 크림색 배경 */
  --paper: #fefcf7;       /* 카드 배경 */
  --ink: #1a1612;         /* 본문 텍스트 */
  --ink-light: #4a4540;   /* 보조 텍스트 */
  --rule: #c8bfaa;        /* 구분선 */
  --accent: #2c4a7c;      /* 포인트 네이비 */
  --accent-light: #d4e0f0;
  --correct: #2d6a4f;     /* 정답 초록 */
  --correct-bg: #d8f3dc;
  --wrong: #9b2335;       /* 오답 빨강 */
  --wrong-bg: #fde8ea;
  --selected: #1a3560;    /* 선택됨 파랑 */
  --selected-bg: #dce8f7;
}
```

**폰트**: `@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@300;400;500;600;700&family=Noto+Sans+KR:wght@300;400;500;700&display=swap');`

**레이아웃**: `max-width: 900px`, `margin: 0 auto`, 충분한 padding

---

## STEP 4. 선택지 상태 클래스 (4가지)

| 상태 | 클래스 | 시각 효과 |
|---|---|---|
| 선택 전 | (없음) | 흰 배경, hover 시 파란 테두리 |
| 선택됨 (채점 전) | `.selected` | 파란 배경 |
| **정답 선택** | `.correct` | 초록 배경 + 펄스 애니메이션 |
| **오답 선택** | `.wrong` | 빨간 배경 + 흔들기 애니메이션 |
| 정답 공개 | `.reveal-correct` | 초록 배경 반투명 (오답 선택 시 정답 표시용) |

---

## STEP 5. JavaScript 핵심 로직

```javascript
const state = {
  answers: {},   // { 문항번호: 선택한번호 }
  score: 0,
  total: 문항수
};

// 정답 데이터
const CORRECT = {
  1: 정답번호,
  2: 정답번호,
  // ...
};

// 해설 데이터
const EXPLANATIONS = {
  1: {
    correct: "정답 해설 텍스트",
    wrong: (chosen) => `오답 안내 텍스트. 정답은 N번입니다.`
  },
  // ...
};

// 핵심 함수
function answer(qId, chosen, correct) {
  if (state.answers[qId] !== undefined) return; // 중복 클릭 방지
  state.answers[qId] = chosen;
  const isCorrect = chosen === correct;
  if (isCorrect) state.score++;

  // 1. 모든 버튼 비활성화
  // 2. 선택한 버튼에 .correct 또는 .wrong 적용
  // 3. 오답이면 정답 버튼에 .reveal-correct 적용
  // 4. 결과 바 텍스트 설정 후 .show 추가
  // 5. 상태 아이콘 업데이트 (✓ 또는 ✗)
  // 6. 점수 갱신
  // 7. 전체 완료 시 최종 결과 표시
}

function resetAll() {
  // 모든 상태 초기화
  // 버튼 클래스/disabled 초기화
  // 결과 바 숨김
  // 아이콘 ○로 복원
  // 최종 결과 숨김
}
```

---

## STEP 6. 애니메이션 정의

```css
@keyframes flash-correct {
  0%   { transform: scale(1); }
  40%  { transform: scale(1.02); }
  100% { transform: scale(1); }
}
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  20%       { transform: translateX(-6px); }
  60%       { transform: translateX(6px); }
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(4px); }
  to   { opacity: 1; transform: none; }
}
```

---

## STEP 7. 최종 결과 격려 메시지 배열

```javascript
const msgs = [
  '다시 한번 도전해 보세요! 💪',      // 0점
  '조금 더 힘내세요! 📚',              // 25%
  '절반 이상 맞았어요! 🌱',            // 50%
  '잘했어요! 복습하면 완벽해요 ⭐',    // 75%
  '훌륭합니다! 거의 다 맞았어요 🎯',  // 거의 만점
  '만점! 완벽한 이해입니다! 🏆'        // 만점
];
```

---

## 출력 요구사항

- **단일 .html 파일**로 출력 (CSS, JS 모두 인라인)
- 외부 라이브러리 없음 (Google Fonts CDN만 허용)
- 모바일/데스크톱 반응형
- 한국어 word-break: keep-all 적용
- 수식은 `<em>` 태그로 이탤릭 처리 (MathJax 없이)
- 〈보기〉가 있는 문항은 반드시 별도 박스로 구분
- 밑줄 조건(㉠~㉤)은 `<span class="cond">` 처리

---

## 체크리스트 (생성 후 확인)

- [ ] 지문 본문이 완전히 포함되었는가?
- [ ] 각 문항의 정답이 올바르게 설정되었는가?
- [ ] 정답/오답 클릭 시 시각 피드백이 작동하는가?
- [ ] 이미 답한 문항을 다시 클릭해도 변경되지 않는가?
- [ ] 모든 문항 완료 후 최종 결과가 표시되는가?
- [ ] 초기화 버튼이 완전히 리셋하는가?
- [ ] 각주(※)가 포함되었는가?
- [ ] 수학 기호/수식이 올바르게 표시되는가?
```

---

## 🔑 AI별 사용 팁

### Claude (claude.ai)
- PDF 파일을 직접 첨부하면 가장 잘 읽음
- "위 프롬프트대로 첨부한 PDF를 HTML로 만들어줘" 라고 입력

### ChatGPT (GPT-4o)
- PDF를 첨부하거나, 텍스트로 복사해서 프롬프트 뒤에 붙여넣기
- "코드 블록 없이 전체 HTML을 출력해줘" 추가 요청

### Google Gemini
- Google Drive에 PDF 업로드 후 링크 공유 방식 사용
- 마찬가지로 프롬프트 + 내용 첨부

---

## 📌 자주 쓰는 추가 지시 (상황별)

| 상황 | 추가할 문장 |
|---|---|
| 그림/도식이 복잡한 경우 | "그림은 HTML/CSS로 최대한 재현하고, 재현이 불가한 경우 [그림] 플레이스홀더를 사용하세요." |
| 빈칸 추론 문제 포함 시 | "빈칸(_____)은 밑줄 태그로 표시하고, 선택지에서 맞는 표현을 고르는 방식으로 구성하세요." |
| 수학 수식이 많은 경우 | "복잡한 수식은 MathJax CDN을 추가하여 렌더링하세요." |
| 배점 표시 원하는 경우 | "각 문항 헤더에 배점(예: [3점])을 함께 표시하세요." |
| 타이머 기능 원하는 경우 | "헤더에 카운트다운 타이머를 추가하세요. 기본 시간은 문항당 2분으로 설정하세요." |

---

*이 프롬프트는 EBS 수능특강 스타일의 국어 지문 기반으로 최적화되어 있습니다.*
*과학탐구, 사회탐구 등 다른 과목에도 동일하게 적용 가능합니다.*
